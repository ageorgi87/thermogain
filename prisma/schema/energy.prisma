// Energy Price Cache - External API data caching
// Stores Mean Reversion model data from DIDO API
// Data is refreshed monthly to ensure up-to-date pricing projections
// Single source of truth for energy price evolution models

model EnergyPriceCache {
  id         String @id @default(cuid())
  energyType String @unique // "fioul", "gaz", "electricite", "bois"

  // Mean Reversion Model parameters
  // recentRate: Recent rate influenced by current market (ex: 8.7% for gas)
  // equilibriumRate: Long-term equilibrium rate (ex: 3.5% for gas)
  // transitionYears: Transition period in years (typically 5)
  recentRate       Float @default(0)
  equilibriumRate  Float @default(0)
  transitionYears  Int   @default(5)

  // Legacy field (deprecated, kept for compatibility)
  currentPrice Float @default(0)

  lastUpdated DateTime @default(now())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
